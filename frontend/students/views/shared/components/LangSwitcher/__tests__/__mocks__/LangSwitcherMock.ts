import { ILocaleOption } from '../../LangSwitcher';

export const mockEnLocale: ILocaleOption = {
  lKey: 'en',
  labelKey: 'frontend.locale.locales.en',
  flagSrc: { default: '' }
};

export const mockBgLocale: ILocaleOption = {
  lKey: 'bg',
  labelKey: 'frontend.locale.locales.bg',
  flagSrc: { default: '' }
};
