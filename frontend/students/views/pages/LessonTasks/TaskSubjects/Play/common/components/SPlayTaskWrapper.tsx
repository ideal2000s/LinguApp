import styled from 'styled-components';

const SPlayTaskWrapper = styled.div`
  color: white;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
`;

export default SPlayTaskWrapper;
